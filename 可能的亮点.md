0 模仿Behavior  Designer 自己实现了行为树架构，支持各种组合节点和Abort Self 打断机制（对selector，检查自身下方的节点是否有为真的条件节点，有则调回来执行；对sequence，检查是否有为假的节点，有则返回Failure）

1 在计算无球跑位时，采用效用体系。

防守时，定义了  保持自身阵型位置，压迫持球人，切断传球路线，与球的距离的权重 ；

在进攻时，定义了 保持自身阵型位置，找到空间，接应持球人，与球门距离的权重。

为后卫和前锋赋值权重大小不同，表现出不同的跑位行为。

 统计不同角色的计算架构